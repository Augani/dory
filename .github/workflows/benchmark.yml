name: Benchmark

# Memory benchmark: Dory's shared VM vs one VM per container.
#
# IMPORTANT: this needs real Mac hardware with hardware virtualization. GitHub-HOSTED macOS runners
# are themselves VMs without nested virtualization, so they CANNOT run the container engine. This
# job produces Dory/Docker/OrbStack numbers on a SELF-HOSTED macOS 15+ runner with Dory installed.
# Apple Container comparison rows require macOS 26+ and are N/A on Intel macOS 15. Label such a
# runner `[self-hosted, macOS, dory]`; add `intel` to the runner labels for the Intel track.

on:
  workflow_dispatch:
    inputs:
      count:
        description: 'Number of containers'
        default: '2'
      image:
        description: 'Image to run'
        default: 'alpine:latest'
  schedule:
    - cron: '0 9 * * 1'  # weekly, Monday 09:00 UTC

jobs:
  benchmark:
    runs-on: [self-hosted, macOS, dory]
    steps:
      - uses: actions/checkout@v4
      - name: Run memory benchmark
        run: scripts/benchmark.sh "${{ github.event.inputs.count || '2' }}" "${{ github.event.inputs.image || 'alpine:latest' }}"
      - name: Summarize
        run: |
          {
            echo "### Dory memory benchmark"
            echo ""
            echo '```json'
            cat benchmark-results.json
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: benchmark-results.json
