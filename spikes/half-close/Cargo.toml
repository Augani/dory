[package]
name = "half-close-spike"
version = "0.0.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["net", "io-util", "rt-multi-thread", "macros", "time"] }

[[bin]]
name = "half-close-spike"
path = "src/main.rs"
