# Linux VM performance bundle schema-1 test fixture

This fixture exists only to exercise the fail-closed bundle verifier. Its frozen value is a test
value, not a Dory product budget. The Ed25519 public key and detached signature are test-only trust
material derived for this fixture; they MUST NOT be used by release qualification or production
signing.
